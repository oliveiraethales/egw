<p>Items for <%= @subject.name %></p>

<div class="row">
  <div class="col-sm-4 col-md-4">
    <ul class="list-group">
      <% @subject.items.each do |i| %>
        <li class="list-group-item">
          <%= references(i.text) %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
